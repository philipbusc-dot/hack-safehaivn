DATABASE_URL="file:./dev.db"
ALLOW_ORIGIN=http://localhost:5173
PORT=3000

# Optional — when set, the AI Survival Briefing uses OpenAI.
# Leave blank to use the built-in templated briefing generator (works offline).
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
